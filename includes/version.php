<?php /* $Id$ $URL$ */

// Set the version components separately.
$w2p_version_major = 2;
$w2p_version_minor = 0;
$w2p_version_patch = 1; // Can be set to null if not used
$w2p_version_prepatch = pre; // Set to null if a release version.